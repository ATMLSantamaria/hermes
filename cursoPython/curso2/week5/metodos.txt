METODOS
STRINGS
:s/sumador/sm/g -> encontrar y reemplazar en todo el texto
#con bin
var.lstrip() ->elimina espacios del principio
var.rstrip() ->elimina espacios del final
var.strip() ->elimina espacios del principio y final
Prefixes
fine="Hola que tal"
fine.startswith("Hola") ->Devuelve true o falsa

fine.endswith("tal")->Devuelve true o false

line=a.martinezl@alumnos.upm
arrobaposicion=line.find("@")
puntoposicion=line.find(".")
host=data[arrobaposicion+1:puntoposicion)

FILES
FHAND=OPEN('mbox.txt')
print(fhand)
<open file 'metodos.txt', mode 'r' at 0x108efb6f0>

newline Character
\n
print("HELLO\nWORLD")
HELLO
WORLD
for i in fhand:
	blablabla
Esto recorre el archivo fila a fila
inp=fhand.read()
lee todo el archivo y lo coloca en un string
print () añade un \n, es decir, inicia una nueva linea cada vez que se ejecuta
Si ademas en el final de una linea del texto hay un \n va a abajar 2 espacios
Podemos evitar esto si hacemos
fhand=open("metodos","r")
for line in fhand:
	line=line.rstrip() ->elimina el espacio del final o la nueva linea
	if line.startwith("bla")
		blabla
LISTS
list=[]
for i in list:
	print (i)
longitud=len(list)
range(len(list)) devuelve [0,1,2,...,len-1]
for i in list:
	bla bla
for i in range(len(list)):
	blabla
	#counted loop, en cada i ademas
	# sabemos la posición denteo de la lista
list.append()
stuff=lista() -> lista vacia
lista.append(99) -> añado 99 a una lista vacia
some=[1,2,3,4,5]
3 in some
True
10 not in some
True
friends=["Alejandro","Teodoro","Marcelino","Rocio","Hanna"]
friends.sort() -> ordena la lista
print(max(some))
print(min(some))
print(sum(some))
split()
abc="with three words"
stuff=abc.split()split break a string into parts
split se puede usar con otras cosas que no sean espacios, por ejemplo
frases=libro.split('.')
print(stuff)
['with','three','words']

DICCIONARIOS
purse=dict()
purse['key']=value
purse['money']=12
metodo get->comprueba si un valor está ya en el diccionario, y da el valor por defecto si la clave no está presente
count=diccionario
name=key
x=count.get(name,0) name va a ser el valor key y 0 es el valor por defecto que devuelve si no encuentra la clave
metodo items
jjj.items() -> da la lista key,value en forma de una tupla
se puede iterar en un diccionario con dos variables
jjj={'chuck':1,'fred':42,'jan':100}
for aaa,bbb in jjj.items() :
	print(aaa,bbb)
Como jjj.items devuelve los pares key, value 
Eeste bucle for con 2 variables itera a traves de key y value a la vez
Decir variable=None la cataloga como tipo None, es un tipo en si mismo, y sirve por ejemplo para hallar comparaciones entre numeros, para establecer una excepcion en el primer numero (que no tiene con que ser comparado)

